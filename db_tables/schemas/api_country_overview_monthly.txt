(
    country_or_region VARCHAR(100),
    country_code VARCHAR(3),
    "date" DATE,
    demand_twh DECIMAL(14,2),
    emissions_intensity_gco2_per_kwh DECIMAL(14,3),
    continent VARCHAR(20),
    ember_region VARCHAR(100),
    eu_flag TINYINT,
    g20_flag TINYINT,
    g7_flag TINYINT,
    oecd_flag TINYINT,
    world_demand_rank INT,
    region_demand_rank INT,
    oecd_demand_rank INT,
    eu_demand_rank INT,
    latest_year INT,
    coal_deadline INT,
    clean_deadline INT,
    PRIMARY KEY(country_or_region, "date")
)